version: "3.8"

services:
  app:
    image: ${IMAGE_NAME}
    environment:
      - API_URL=${API_URL}
      - API_TOKEN=${API_TOKEN}
    restart: always
    container_name: next-prod
    ports:
      - 3001:3001
    deploy:
      mode: replicated
      replicas: 2